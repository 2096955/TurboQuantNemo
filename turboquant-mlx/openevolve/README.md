# OpenEvolve (Phase 6) — pointer

The runnable scaffold lives in **`../turboquant_mlx_kernel_evolution/`** (evaluator,
`initial_program.py` with `EVOLVE-BLOCK`, `config.yaml`, launcher script, subprocess
hook smoke test). See that directory’s `README.md`.

Upstream reference: [OpenEvolve](https://github.com/algorithmicsuperintelligence/openevolve)
and `examples/mlx_metal_kernel_opt/`.
