[pytest]
# So `pytest tests/...` works without editable install or PYTHONPATH=.
pythonpath = .
testpaths = tests
# Per-test 300s limit is applied in tests/conftest.py via pytest-timeout (see setup.py [test]).
